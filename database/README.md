# Banco de Dados

Use estes arquivos no SQL Editor do Supabase:

1. `schema.sql` — cria/ajusta as tabelas, índices e permissões.
2. `seed.sql` — carrega os dados importados/OCR dos formulários de defeito, se necessário.

Os antigos SQLs de correção foram consolidados para deixar o projeto mais limpo.
